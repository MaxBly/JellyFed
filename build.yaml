---
name: "JellyFed"
guid: "ff0164d7-e8c0-44c1-bc61-45017125a155"
version: "0.1.0.18"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Native federation between Jellyfin instances"
description: >
  JellyFed connects multiple Jellyfin servers together. Libraries from
  federated instances appear as native items — with artwork, metadata
  and direct streaming — in any Jellyfin client, without modification.
category: "General"
owner: "MaxBly"
artifacts:
- "Jellyfin.Plugin.JellyFed.dll"
changelog: >
  Replace the experimental sources.json/IMediaSourceProvider flow with the v2 flattened
  multi-version layout, generate one .strm/.nfo per peer source, prefer SelfUrl for
  advertised stream URLs, and keep peer pruning scoped to per-peer materialized files.
